account.hh
--
//Messages the middle tier either sends to or receives from the back tier.
middleBack
  -out -noInline (messageID,::std::vector<cmwAccount>,::int32_t)
  -out -maxLength=700000 (messageID,cmwRequest)
  -out (messageID)
}

middleFront
  -out -maxLength=udp_packet_max -noInline -yeaNay (bool,stringPlus)
}
